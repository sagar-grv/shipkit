# ShipKit — Validate templates and docs
name: Validate

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-templates:
    name: Validate Templates
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      
      - name: Check template files
        run: |
          echo "Checking template files exist..."
          errors=0
          for f in \
            template/agents/planner.md \
            template/agents/co-developer.md \
            template/agents/security-reviewer.md \
            template/agents/monitor.md \
            template/docs/AGENTS.md \
            template/docs/ROADMAP.md \
            template/docs/BUGS.md \
            template/docs/LAST_SESSION.md \
            template/github/dependabot.yml \
            template/github/workflows/ci.yml \
            template/github/workflows/codeql.yml \
            template/github/workflows/playwright.yml \
            template/husky/pre-commit; do
            if [ -f "$f" ]; then
              echo "  ✓ $f"
            else
              echo "  ✗ MISSING: $f"
              errors=$((errors + 1))
            fi
          done
          if [ $errors -gt 0 ]; then
            echo "ERROR: $errors template files missing!"
            exit 1
          fi
          echo "All template files OK"

  check-readme:
    name: Check README
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      
      - name: Verify README exists
        run: |
          test -f README.md && echo "✓ README.md exists"
          test -f LICENSE && echo "✓ LICENSE exists"
          test -f setup.ps1 && echo "✓ setup.ps1 exists"
          test -f setup.sh && echo "✓ setup.sh exists"
